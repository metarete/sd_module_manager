<form method="post" action="{{ path('app_parere_mmg_delete', {'id': parere_mmg.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parere_mmg.id) }}">
    <button class="btn btn-danger">Elimina</button>
</form>
