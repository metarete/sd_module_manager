<form method="post" action="{{ path('app_lesioni_delete', {'id': lesioni.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lesioni.id) }}">
    <button class="btn btn-danger">Elimina</button>
</form>
