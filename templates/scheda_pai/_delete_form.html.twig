<form method="post" action="{{ path('app_scheda_pai_delete', {'id': scheda_pai.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scheda_pai.id) }}">
    <button class="btn btn-danger">Elimina scheda</button>
</form>
