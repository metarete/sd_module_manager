<form method="post" action="{{ path('app_tinetti_delete', {'id': tinetti.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tinetti.id) }}">
    <button class="btn btn-danger">Elimina</button>
</form>
