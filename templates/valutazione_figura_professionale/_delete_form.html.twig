<form method="post" action="{{ path('app_valutazione_figura_professionale_delete', {'id': valutazione_figura_professionale.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ valutazione_figura_professionale.id) }}">
    <button class="btn btn-danger">Elimina scheda</button>
</form>
