<form method="post" action="{{ path('app_vas_delete', {'id': va.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ va.id) }}">
    <button class="btn btn-danger">Elimina scheda</button>
</form>
