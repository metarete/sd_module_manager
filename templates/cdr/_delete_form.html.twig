<form method="post" action="{{ path('app_cdr_delete', {'id': cdr.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cdr.id) }}">
    <button class="btn btn-danger">Elimina</button>
</form>