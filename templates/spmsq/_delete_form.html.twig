<form method="post" action="{{ path('app_s_p_m_s_q_delete', {'id': s_p_m_s_q.id}) }}" onsubmit="return confirm('Sei sicuro di voler cancellare questa scheda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s_p_m_s_q.id) }}">
    <button class="btn btn-danger">Elimina</button>
</form>
