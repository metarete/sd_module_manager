{{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
<div class="row">
	<div class="col">
		{{ form_row(form.dataValutazione) }}
	</div>
</div>
<div class="row">
	<div class="col">
		{{ form_row(form.conclusioni) }}
	</div>
</div>
<div class="row">
	<div class="col col-lg-1">
		{{ form_row(form.rinnovo) }}
	</div>
    <div class="col col-lg-1">
    <a class="bi bi-info-circle-fill" href="#" data-toggle="tooltip" title="Selezionando rinnovo sarà necessario creare un nuovo progetto dopo la chiusura di questa scheda pai"></a>
    </div>
</div>
<button class="btn btn-primary">{{ button_label|default('Salva') }}</button>
{{ form_end(form) }}
